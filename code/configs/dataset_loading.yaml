data_config:
  dataset_config:
    data_path: "/home/manolotis/sandbox/robustness_benchmark/lstmAutoencoder/data/prerendered/training/"
    n_shards: 1
    input_data: [ "xy", "yaw", "v_xy", "valid" ]
  dataloader_config:
    batch_size: 42
    shuffle: False
    num_workers: 8

